{
  "name": "starweft",
  "version": "0.1.0",
  "private": true,
  "description": "STARWEFT — an original sandbox space MMO. Monorepo: deterministic core, protocol, authoritative server, client SDK, terminal client.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm run build && pnpm -r run test",
    "bench": "pnpm -r --if-present run bench",
    "typecheck": "pnpm -r run typecheck",
    "validate:content": "pnpm --filter @starweft/core run validate:content",
    "check:determinism": "node tools/check-determinism.mjs",
    "play": "pnpm --filter @starweft/cli run build && pnpm --filter @starweft/cli run start"
  }
}
